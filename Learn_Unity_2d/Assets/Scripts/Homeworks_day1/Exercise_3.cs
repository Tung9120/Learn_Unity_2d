using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Learn_Unity_2d
{
    public class Exercise_3 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            // Bài 3: Tính diện tích, chu vi hình tròn

            int R = 10;
            float Pi = 3.14f;

            // Debug.Log("Bai 3:");
            // Debug.Log("Chu vi hinh tron: " + (R * 2 * Pi));
            // Debug.Log("Dien tich hinh tron: " + (R * R * Pi));
        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}