using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Learn_Unity_2d
{
    public class Exercise_1 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            // Bài 1: Tính trung bình cộng của bốn số bất kỳ

            int a = 1;
            float b = 9.2f;
            int c = 6;
            float d = 27.6f;

            // Debug.Log("Bai 1:");
            // Debug.Log("Tinh trung binh cong cua 4 so: " + ((a + b + c + d) / 4) + "\n");
        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}